// //kth highest element in array using priority queue use maxQueue.
// //kth smallest element in array using priority queue use minQueue.

// **K-th largest element**
// --> To solve this problem , we are using priority queues according to the problem statement use maxQueue or min Queue.
// --> Keep on pushing the element into queue.
// --> if no of elements is greater than k.(size of queue) then pop the elements
// -->as we keep on pushing the element into the queue. As we have taken Max_queue the elements will be in descending order.At the end of the iteration the top element will be the kth largest element.(my target value will be floated).then get the top element .Expected output will be printed.

class SecondHighestElement
{
  public static void main(String args[])
 {
  
 }
}